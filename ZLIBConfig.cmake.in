# Compute paths
get_filename_component(ZLIB_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(ZLIB_INCLUDE_DIR "@CONF_INCLUDE_DIRS@")
 
# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET foo AND NOT ZLIB_BINARY_DIR)
  include("${ZLIB_CMAKE_DIR}/ZLIBTargets.cmake")
endif()

set(ZLIB_NAMES zlib)
#set(ZLIB_LIBRARY)
#foreach(ZLIB_NAME ${ZLIB_NAMES}) 
  get_target_property(ZLIB_LIB_FILENAME ${ZLIB_NAME} IMPORTED_LOCATION_NOCONFIG)
  set(ZLIB_LIBRARY ${ZLIB_LIB_FILENAME})
#endforeach()


